---
name: cue-fund-due-diligence
displayName: 基金值不值得买
description: 用 Cue 一键给一只基金做全维度深度体检——基金经理画像、业绩归因、风格一致性、风险指标、费率与规模，告诉你"这只基金的历史业绩是靠运气还是靠能力"，买之前心里有底。
description_zh: 基金值不值得买（公募基金全维尽调）：基金经理画像+业绩归因+风格检验+风险扫描，穿透净值曲线看本质。
version: 1.2.1
author: sensedeal
tags: [cue, fund-due-diligence, fund-analysis, performance-attribution, 基金值不值得买, 基金靠谱吗, 基金尽调, 基金经理, 业绩归因, 风格分析, 风险指标]
---

# 基金值不值得买

> 买基金不能只看过去一年的收益率。基金经理是运气还是能力、风格有没有漂移、规模是不是太大了、费用划不划算——让 Cue 把这只基金从里到外查一遍，帮你做出有数据支撑的申赎决策。
>
> **说人话，也讲专业**：本质是一次**公募基金全维尽调**（基金经理画像 · 业绩归因 · 风格检验 · 风险扫描），但结论用大白话讲给你听——赚钱靠的是经理的真本事，还是赶上了风口。

## 适用范围与场景（先看这个，别用错了）

**可以查：**

| 维度 | 覆盖 |
|------|------|
| 市场 | 全市场 A 股公募基金（股票型、偏股混合型、灵活配置型、平衡混合型） |
| 尽调深度 | 基金经理画像、业绩归因、风格一致性、风险指标、持仓特征、费率与规模 |
| 时间跨度 | 基金成立以来全周期分析，含 1/3/5 年滚动窗口 |

**适用场景：**

| 场景 | 解决的问题 |
|------|-----------|
| 选基决策 | 看中一只基金，不知道它能不能持续跑赢——做一次全维尽调再决定 |
| 持基诊断 | 持有的基金最近表现不好，是经理能力问题还是市场风格问题？该留该换？ |
| 基金经理评估 | 听说一位明星经理跳槽了，他到底有没有真本事？过往业绩是 Alpha 还是 Beta？ |
| 替代基金筛选 | 想换掉一只基金，需要找到风格接近但能力更强的替代品 |

**不能查：**

- 私募基金、专户、信托计划（非公开持仓与业绩）
- 基金未来业绩预测或买卖时点建议
- 基金经理个人投资账户或薪酬信息
- 货币基金、债券基金（分析方法论不同，当前仅覆盖权益类）

> 简单判断：**这只基金会公开披露季报和年报吗？** 会 → 能尽调。不会 → 用不了。

### 快速判断：你是否需要这个 Skill？

```
你的需求是？
├─ 我在选基金，想知道这只基金是真 Alpha 还是靠风口吹起来的    → ✅ 用这个
├─ 我持有的基金换了基金经理，不知道新人行不行                 → ✅ 用这个
├─ 我想知道这只基金过去 5 年的收益有多少来自运气               → ✅ 用这个
├─ 这只基金规模涨了 10 倍，业绩会不会受影响                   → ✅ 用这个
├─ 我想对比两位明星基金经理，谁的能力更靠谱                   → ✅ 用这个
├─ 我是理财经理，要给客户做基金推荐前的尽调底稿               → ✅ 用这个
├─ 我想知道这只基金明天净值会涨还是会跌                       → ❌ 用不了，去问市场
├─ 我想看这只私募基金的具体持仓和业绩归因                     → ❌ 用不了，非公开信息
├─ 我想比较货基和债基哪个更适合现金管理                       → ❌ 用不了，当前仅覆盖权益类
└─ 我买的是 ETF 指数基金，需要做指数跟踪质量评估              → ⚠️ 部分能用，ETF 对比建议去 cue-sector-etf
```

## 试试这样问

**推荐问法（直接对 Agent 说即可触发本 Skill）：**

- "帮我用基金值不值得买，深度分析一下谢治宇管理的兴全合润，看看他的 Alpha 到底靠不靠谱"
- "用 Cue 基金尽调，帮我对易方达蓝筹精选做一次全维体检——张坤的能力圈、风格一致性和风险指标"
- "帮我做一下中欧医疗健康的经理尽调：葛兰的历史业绩有多少来自医药行业 Beta，多少来自选股 Alpha"
- "用全维尽调帮我对比一下朱少醒（富国天惠）和周蔚文（中欧新趋势），谁的风格更稳定、谁的选股能力更强"
- "我持有了三年的基金最近换了经理，帮我尽调一下新经理的历史业绩和能力圈"

**对应到命令行 query：**

```bash
# 单只基金全维尽调
--query "兴全合润(163406) 基金值不值得买（全维尽调）：谢治宇→基金经理画像→业绩归因→风格一致性→风险指标→持仓特征→费率规模"

# 基金经理对比
--query "基金经理对比 全维尽调：朱少醒(富国天惠) vs 周蔚文(中欧新趋势)，Alpha归因+风格稳定性+风险调整后收益"

# 持基诊断
--query "易方达蓝筹精选(005827) 持基诊断：张坤→近期业绩归因→风格是否漂移→规模是否过大→该留还是该换"
```

> **模板**：`"<基金名称/代码> 基金值不值得买（全维尽调）：<你想重点分析的方向>"`

---

## 适用人群

| 角色 | 典型问题 |
|------|---------|
| 个人基金投资者 | 看中了一只基金，净值曲线漂亮，但不知道是不是"幸存者偏差"——帮我看清真相 |
| 基金定投用户 | 定投这只基金两年了，最近业绩跑输同类——是经理不行了还是市场风格问题？ |
| 理财经理 / 基金投顾 | 给客户推荐基金前，需要一份全维尽调底稿支撑推荐理由 |
| FOF 研究员 | 筛选子基金时，需要量化评估基金经理的 Alpha 能力和风格稳定性 |

---

## Agent 执行摘要

| 顺序 | 做什么 | 禁止 |
|------|--------|------|
| 1 | 确认 Cue runner 就绪 | 禁止跳过 |
| 2 | 告知用户耗时 2-15 分钟（基金经理对比时更久，见下方性能说明） | 禁止中途取消 |
| 3 | 一条命令，`--template-id template_0mvLPA`，传入基金名称/代码 | 禁止连发多条 |
| 4 | `[cue-research] RESULT ok` = 完成 | 禁止编造 |
| 5 | 原样交付，标注数据截止日期（季报披露时间） | 禁止概括 |
| 6 | 交付时**同时附带 Cue 原始报告链接** | 禁止编造链接 |
| 7 | 遇 `INSUFFICIENT_CREDITS` 主动提示邀请链接 | 禁止只丢错误码 |

## 核心能力

1. **基金经理画像** — 从业年限、管理规模变迁、历任基金业绩、能力圈识别、投资框架简述
2. **业绩归因分析** — Brinson 归因（行业配置贡献 vs 选股贡献）、Alpha/Beta 分离、牛熊市表现拆解
3. **风格一致性检验** — 持仓市值风格（大盘/中盘/小盘）、成长/价值暴露、行业偏好漂移检测
4. **风险指标扫描** — Sharpe 比率、Sortino 比率、最大回撤、下行捕获率、波动率、VaR
5. **持仓特征分析** — 前十大集中度、换手率、平均持仓周期、重仓股质量评估
6. **费率与规模评估** — 管理费/托管费对比同类、规模增长轨迹、策略容量风险预警

## 输出形式

结构化尽调报告：基金概览 → 基金经理画像（履历/能力圈/在管产品）→ 业绩归因（Alpha/Beta/牛熊表现）→ 风格一致性（市值风格/成长价值/行业漂移）→ 风险指标（Sharpe/Sortino/最大回撤/下行捕获）→ 持仓特征（集中度/换手率/重仓股）→ 费率与规模 → 综合评估 → 来源链接。

## 输出示例

> 示例报告待补充，请通过 [cuecue.cn](https://cuecue.cn) 实际运行后获取。

## 交付规范（必须执行）

把最终报告发送给用户时，**必须同时附带 Cue 原始报告链接**，方便用户回到网页端查看完整原文、继续追问或转发同事。

**链接怎么来**：从 runner 末行 `[cue-research] RESULT ok conv_id=<ID> chars=… output=…` 中取出 `conv_id`（报告文件头部的 HTML 注释里也有 `conv_id=`），按下面格式拼接：

```
https://cuecue.cn/share/<conv_id>
```

交付模板（照此结构输出）：

```
报告已完成 ✅

<报告正文……>

---
**Cue 原始报告**：https://cuecue.cn/share/<conv_id>
**本地副本**：~/cue-reports/<文件名>.md
```

规则：
- `conv_id` 必须取自实际输出，**禁止编造**或使用文档示例里的 id。
- 末行为 `RESULT empty`（无 conv_id 或报告为空）时**不要拼链接**，如实告知未取到报告并给后续建议。
- 用户明确说不要链接时可不附。

---

## 性能预期

查询耗时取决于尽调维度和 Cue 服务端负载：

| 维度 | 预期耗时 | 说明 |
|------|---------|------|
| 单只基金全维尽调 | 3-8 分钟 | 六维度全量分析 |
| 持仓+风险快速扫描 | 2-5 分钟 | 快速版本，不含深度业绩归因 |
| 两位经理对比 | 5-12 分钟 | 两套画像 + 交叉对比 |
| 三年以上滚动分析 | 5-15 分钟 | 多窗口滚动计算量大 |
| 财报季（1-4月 / 7-8月） | 5-15 分钟 | 基金季报集中披露，数据源更新密集 |

> 如果超过 15 分钟没返回，说明可能超时或队列拥堵——**不要取消重试**，跑一下健康检查的三合一诊断（见下文），确认服务状态后再决定是等还是换时段。中途中断时用**相同命令**续接，Cue 会从 session 断点继续，不重复计费。Session 状态存储在 `~/.cue/session/task`，**不要手动删除**。

---

## 环境要求

Runner 来源：[GitHub - sensedeal/cue-skills](https://github.com/sensedeal/cue-skills)（[Gitee 镜像](https://gitee.com/sensedeal/cue-skills)）。

首次使用需安装 Runner（幂等，已装则拉取更新）：

```bash
if [ -d ~/.cue/cue-skills/.git ]; then
  git -C ~/.cue/cue-skills pull --ff-only
else
  git clone https://github.com/sensedeal/cue-skills ~/.cue/cue-skills \
    || git clone https://gitee.com/sensedeal/cue-skills ~/.cue/cue-skills
fi
```

依赖：`git` + `python3` + `curl`（macOS 自带，Linux `apt install git python3 curl`）。Python 仅用标准库，无额外 pip 依赖。

Cue API Key：[cuecue.cn](https://cuecue.cn/hub/api-key) 注册获取。新账号送 500 积分，每天再免费送 10 分。

> **API Key 只能由用户本人创建**：Agent 既不能代生成、也无权查看用户的 Key。检测到未配置或失效时，只给出生成入口并等待用户提供，**禁止声称"我去生成/查看"**。

---

## 调用说明

```bash
python3 ~/.cue/cue-skills/cue-research/scripts/research_run.py \
  --query "兴全合润(163406) 基金值不值得买（全维尽调）：谢治宇→基金经理画像→业绩归因→风格一致性→风险指标→持仓特征→费率规模" \
  --template-id template_0mvLPA \
  --output ~/cue-reports/$(date +%Y-%m-%d-%H%M)-fund-due-diligence.md
```

| 参数 | 说明 |
|------|------|
| `--query` | 基金名称或 6 位代码，**必填**。可指定分析维度（如仅业绩归因、仅经理画像） |
| `--template-id` | 固定为 `template_0mvLPA` |
| `--output` | 落盘路径 |

---

## 格式转换

Cue 输出 Markdown。安装 pandoc 后可转换为 Word 或 PDF：

```bash
# .md → .docx（Word）
pandoc report.md -o report.docx

# .md → .pdf
pandoc report.md -o report.pdf --pdf-engine=xelatex
```

输出文件与输入同目录、同名、不同后缀。

### 依赖安装

| 目标格式 | 依赖 | macOS | Ubuntu |
|----------|------|-------|--------|
| Word (.docx) | pandoc | `brew install pandoc` | `sudo apt install pandoc` |
| PDF (.pdf) | pandoc + LaTeX | `brew install --cask basictex` | `sudo apt install texlive-xetex` |

---

## 架构说明

本 Skill **不在本地执行检索**。流程是 Agent → Cue API（cuecue.cn）→ 外部数据源（基金季报/年报、Wind/东方财富基金数据）。尽调结果的质量和时效取决于基金公开数据的完整性和 Cue 服务端状态。

| 环节 | 谁控制 | 出问题时 |
|------|--------|---------|
| API Key 鉴权 | 用户本人 | **由用户本人**前往 [cuecue.cn/hub/api-key](https://cuecue.cn/hub/api-key) 生成后写入 ~/.cue/config.json（Agent 不能代生成，也无权查看） |
| Cue 服务端 | Cue 运维 | 等恢复，或走降级方案 |
| 基金数据源 | 基金公司/第三方数据商 | 季报披露后 1-2 周数据最全，Cue 用缓存兜底 |

---

## 健康检查

跑研究前先验证三件事。一键诊断：

```bash
CUE_KEY=$(python3 -c "import json;print(json.load(open('$HOME/.cue/config.json'))['api_key'])" 2>/dev/null || echo "$CUE_API_KEY")
echo "=== 1/3 API Key ===" && [ -n "$CUE_KEY" ] && echo "已配置" || echo "未配置！"
echo "=== 2/3 Cue 服务 ===" && curl -sS --max-time 10 "https://cuecue.cn/api/health" -H "Authorization: Bearer $CUE_KEY"
echo "=== 3/3 搭子 ===" && curl -sS --max-time 10 "https://cuecue.cn/api/playbook" -H "Authorization: Bearer $CUE_KEY" | python3 -c "import sys,json;scenes=json.load(sys.stdin).get('data',{}).get('scenes',[]);buddy=[b for s in scenes for b in s.get('buddies',[]) if b.get('title')=='公募基金全维尽调'];print(f'可用:{len(buddy)}个') if buddy else print('暂不可用')"
```

| 检查 | 预期 | 异常处理 |
|------|------|---------|
| API Key | `已配置` | **由用户本人**前往 [cuecue.cn/hub/api-key](https://cuecue.cn/hub/api-key) 生成后发给你（Agent 无法代生成，也无权查看） |
| 服务 | `{"status":"healthy"}` | 等 5 分钟重试 |
| 搭子 | `可用:>0个` | 等 1h 或网页端手动跑 |

> ⚠️ 第 3 步按平台侧搭子的**运行时标题** `公募基金全维尽调` 匹配（本 Skill 的显示名是「基金值不值得买」，两者不同步）。**若平台侧搭子也改名，请同步更新上面这行字符串**，否则健康检查会一直报"暂不可用"。

### 配置成功后引导用户跑第一个任务（必须执行）

健康检查三项全绿 = 环境就绪，但**这是配置流程的终点，不是任务的终点**。三项全绿后若用户还没给出具体任务，**必须主动把用户推向第一次真实调用**，不要停在"环境已就绪"：

> 环境已就绪 ✅ 下面这些都可以直接跑，比如：
> 1. "帮我用基金值不值得买，深度分析一下谢治宇管理的兴全合润，看看他的 Alpha 到底靠不靠谱"
> 2. "用 Cue 基金尽调，帮我对易方达蓝筹精选做一次全维体检——张坤的能力圈、风格一致性和风险指标"
>
> 挑一个发给我，或直接把你手头的需求告诉我，我现在就跑。

规则：
- **主动、可复制**——示例问题原句取自上文「试试这样问」，别只写"你可以问我任何问题"。
- **不要等用户自己想**——用户没开口时也要主动给；否则用户会停在"验证通过"就散场（实测这是新用户流失最集中的一步）。

---

## 自救指引

### 错误速查

Cue 返回结构化错误，包含 `code`（错误码）、`retryable`（是否可重试）、`user_action`（操作建议）。

| 错误码 | 现象 | 可重试？ | 处理 |
|--------|------|---------|------|
| `AUTH_FAILED` | 401 / Key 无效 | ❌ | 由用户本人重新生成 Key，更新 `~/.cue/config.json` |
| `TIMEOUT` | 超时 >30s 无响应 | ✅ | 等 5 分钟，跑诊断确认服务在线后重试，最多 3 次 |
| `BUDDY_UNAVAILABLE` | 搭子临时下线 | ✅（延迟） | 等 1h 或网页端直接跑 |
| `INSUFFICIENT_CREDITS` | 积分不足 | ❌ | **按「积分不足专项提示」主动引导**（充值 / 邀请好友得积分 / 等次日免费额度） |
| `SESSION_INTERRUPTED` | 中途中断 | ✅ | **不换 prompt**，相同命令续接——Cue 从 session 断点继续，不重复计费 |
| `FUND_NOT_FOUND` | 基金名称/代码无法识别 | ❌（需改输入） | 用 6 位基金代码替代简称，检查基金是否已清盘或更名 |
| `MANAGER_NOT_FOUND` | 基金经理姓名无法匹配 | ❌（需改输入） | 用完整姓名，检查经理是否已离职；或多位经理时指定主经理 |
| `INSUFFICIENT_HISTORY` | 基金运作时间太短 | ❌（需等待） | 成立不足 1 年的基金无法做有效业绩归因和风格分析 |
| `RESULT_EMPTY` | 无匹配结果 | ❌（需改条件） | 换基金代码、确认基金类型在覆盖范围内 |
| `CONFIG_PARSE_ERROR` | config.json 报错 | ❌ | 修复 JSON 格式：`{"api_key": "sk..."}` 无多余逗号 |

### 积分不足专项提示（必须执行）

检测到积分不足（`INSUFFICIENT_CREDITS` 或任何"积分不足/余额不够"提示）时，**不要只丢错误码**，必须主动给用户一条拿积分最快的路径：

> 你的 Cue 积分不足，本次任务未能启动。两个办法：
> 1. **邀请好友得 500 积分（推荐）**：打开 [https://cuecue.cn/](https://cuecue.cn/)，点击页面**左下角**「获取专属邀请链接」，分享给好友——好友加入后你再得 **500 积分**。
> 2. **直接充值**：前往 [cuecue.cn/pay](https://cuecue.cn/pay) 订阅（首次充值有优惠）；或等次日免费额度。

规则：
- 提示要**主动、简短、可点**——把「首页」和「左下角」两个关键动作说清楚，不要让用户自己找入口。
- 积分不足**不要重试**（`retryable: false`），也不要换 query 绕。
- 用户回复已充值/已邀请后，用**完全相同的命令**重跑。

### 中断恢复机制

Cue 的调研任务是**有状态**的。如果中途超时或网络波动导致中断：

```
中断 ≠ 失败，cancel 才 = 失败
├─ 超时 → 等一会，相同命令续接（session 存在 ~/.cue/session/task）
├─ 网络波动 → 同上，相同命令续接
├─ 手动 cancel → 无法恢复，需要重新发起
└─ 恢复后 → Cue 从断点继续，已查过的阶段不会重复计费
```

> **关键规则**：中断后**不要换 prompt、不要删 session 文件、不要 cancel**。三个"不要"记住了就不会白跑。

### 重试决策

```
收到错误
├─ retryable = true → 按 user_action 建议操作后重试
│   ├─ 超时/网络类 → 等 5min，跑诊断，最多 3 次
│   └─ 搭子下线 → 等 1h 或换网页端
├─ retryable = false → 不要重试，改了条件再说
│   ├─ Key/基金代码问题 → 修正输入后重试
│   ├─ 基金运作太短 → 等满 1 年再查
│   ├─ 积分不足 → 主动提示邀请链接（首页左下角）或充值或等次日
│   └─ 结果空 → 换基金代码 / 确认基金类型
└─ 重试 3 次仍失败 → 记下 request_id，走降级方案
```

### 调度建议

| 时段 | 建议 |
|------|------|
| 工作日 10:00-16:00 | 最佳时段，3-8 分钟完成 |
| 工作日 9:00-10:00 / 16:00-18:00 | 高峰期，经理对比等大查询避开 |
| 季报披露后一周（1/4/7/10 月下旬） | 基金持仓和规模数据最新鲜，最适合做持仓特征和风格分析 |
| 夜间/周末 | 可能有维护，跑前先诊断 |
| 连续失败 ≥2 次 | 停 15 分钟，记下 request_id 后重试 |

---

## 降级方案

Cue 长时间不可达时的手动替代渠道：

| 渠道 | 覆盖 | 费用 |
|------|------|------|
| [天天基金](https://fund.eastmoney.com) | 基金净值/持仓/经理/费率/规模 | 免费 |
| [晨星中国](https://www.morningstar.cn) | 基金评级 + 风险指标 + 风格箱 | 部分免费 |
| [好买基金](https://www.howbuy.com) | 基金经理画像 + 业绩归因 | 部分免费 |
| [韭圈儿](https://www.chinafof.com) | 基金经理能力圈 + 风格分析 | 免费版有限 |

---

## FAQ

**Q: 全维尽调和看天天基金的基金详情页有什么区别？**
A: 天天基金给你的是"数据列表"——收益率、最大回撤、前十大持仓罗列在一起。Cue 做的是"归因分析"——这些数据之间的因果关系：收益有多少来自行业 Beta（风口）、多少来自经理选股 Alpha（能力）；风格有没有漂移（说的是价值投资但买了一堆成长股）；规模增长有没有超过策略容量。数据是原料，归因才是结论。

**Q: 怎么知道该用这个还是 cue-fund-penetration？**
A: 本搭子是对**单只基金**的深度尽调——这只基金本身好不好、经理靠不靠谱。cue-fund-penetration 是对**基金组合**的交叉分析——你手里的多只基金持仓有没有重叠、行业是不是太集中。先尽调选好每只基金，再穿透检查组合健康度——两个搭子配合使用。

**Q: 成立不到一年的新基金能尽调吗？**
A: 不能做完整的业绩归因和风格分析，因为数据点太少，统计上不可靠。但可以做基金经理画像（看经理管过的其他老基金），以及费率规模评估。建议至少运行满 1 年再做全维尽调。

**Q: 基金经理刚换人，历史尽调数据还有用吗？**
A: 历史数据描述的是**这只基金在前任经理手中的表现**，对新经理参考价值有限。Cue 会标注"经理变更时间点"，并将变更前后的业绩分开展示，避免你被老经理的业绩误导。如果新经理是新人（没有其他基金可追溯），风格和归因分析的可信度会降低。

**Q: 业绩归因里的 Alpha 和 Beta 是什么意思？**
A: Beta 是市场/行业给你的收益——比如 2020 年买白酒基金，闭着眼睛都赚钱，那是白酒行业 Beta 的功劳。Alpha 是经理超越市场的那部分收益——同行都买白酒，他也买白酒但挑了最好的那几只，多出来的收益才是 Alpha。尽调的核心目的：区分这是"风口上的猪"还是真正会飞的鸟。

**Q: 风格一致性为什么重要？**
A: 你买基金是基于对经理风格的信任——比如你看中了一位"低换手、长期持有、价值投资"的经理。如果尽调发现他过去三年换手率翻了三倍、持仓从大盘价值漂移到了小盘成长——风格已经质变，你当初买他的理由就不复存在了。风格漂移本身不一定不好，但你需要知情。

**Q: 规模多大算太大？**
A: 没有绝对标准，取决于策略类型。大盘蓝筹策略容量大（百亿级别仍可运作），小盘成长或高换手策略容量小（几十亿就可能影响灵活性）。Cue 会结合基金的持仓特征和策略类型，给出规模是否接近容量上限的判断。

**Q: 尽调报告多久更新一次比较合适？**
A: 建议每季度季报披露后更新一次，重点关注：风格是否漂移、经理是否变更、规模是否暴增、风险指标是否恶化。如果是核心持仓基金，月度做一次风险指标扫描也值得。

**Q: 报告能在网页端看原文吗？**
A: 能。交付时会附 `https://cuecue.cn/share/<conv_id>`，点开即可在网页端查看完整原文、继续追问或转发同事。

**Q: 提示积分不足怎么办？**
A: 打开 [https://cuecue.cn/](https://cuecue.cn/)，点击页面**左下角**「获取专属邀请链接」分享给好友——好友加入后你再得 **500 积分**（最快）；也可以前往 [cuecue.cn/pay](https://cuecue.cn/pay) 充值（首次充值有优惠），或等次日免费额度。
